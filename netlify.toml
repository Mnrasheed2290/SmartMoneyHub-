[build]
  functions = "functions"
  publish = "/"

[[plugins]]
package = "@netlify/plugin-functions-install-core"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["node-fetch"]
  included_files = []

[functions."getPosts"]
  node_bundler = "esbuild"
  included_files = []
